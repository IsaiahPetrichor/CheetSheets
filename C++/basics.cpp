#include <iostream> // import the library to use input and output

int main() { 
    // every cpp file needs a main function, usually an 'int' function that returns 0 when it works properly.
    std::cout << "What's up nerds\n";
    return 0; // finish the function
}